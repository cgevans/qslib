# SPDX-FileCopyrightText: 2021-2025 Constantine Evans <qslib@mb.costi.net>
# SPDX-License-Identifier: EUPL-1.2
version: 2
build:
  os: ubuntu-24.04
  tools:
    python: "3.13"
    # Build the extension from source rather than downloading a CI artifact.
    rust: "1.96"
python:
  install:
    - requirements: docs/requirements.txt
    - method: pip
      path: .
